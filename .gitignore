.DS_Store
node_modules/
.wrangler/
*.log

# Local secrets (Cloudflare convention) — never commit
.dev.vars
.env
.env.*
!.env.example

# Design workbench scratch — kept out of the repo even if a local copy exists.
# (Specs/plans live in-repo under docs/superpowers/ since 2026-08-31.)
design-candidates/
