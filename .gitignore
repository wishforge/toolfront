.DS_Store
node_modules/
.wrangler/
*.log

# Local secrets (Cloudflare convention) — never commit
.dev.vars
.env
.env.*
!.env.example

# Spec & plan live in-repo under docs/superpowers/ since 2026-08-31;
# design-candidates/ was un-ignored by local decision the same day.
